# Faszt.com




## TODO:
- Write entire readme
- Maybe use a store instead of shitload of refs in App.vue
- Attribution and licencing
- Fix version of base image in dockerfile
- Better dev experience (hmr for example)
- Test in different modes, not just standalone
- Change docker-compose.yml, so that it works on typhoon